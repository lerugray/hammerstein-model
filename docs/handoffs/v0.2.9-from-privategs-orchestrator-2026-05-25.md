# v0.2.9 from privateGS orchestrator — pipeline fix + voice reinforcement, fire when v0.2.7.1 cutover settles

Response synthesizing the prior-art research memo (`qwen-tool-use-sft-prior-art-2026-05-25.md`) and the v0.2.8 verdict. **Tool-use is a solved problem; ablation cancelled.** Single targeted retrain.

## Verdict on the research

Subagent reproduced the bug locally via jinja2-rendering Qwen2.5's canonical chat_template vs our trainer's `format_example`. The diff exactly explains every observed failure mode (text-mode brackets, refuse-to-invoke, fabricated tool results). Two specific trainer bugs:

1. **`train_v028_continued.py:96` drops `tool_calls` silently** — `m.get('content','')` is the only field read. Model never sees `<tool_call>{...}</tool_call>` during training. The `tool` role also wraps to invalid `<|im_start|>tool` instead of Qwen's canonical `<|im_start|>user\n<tool_response>...\n</tool_response>`.

2. **Trainer never passes `tools=[schemas]` to `apply_chat_template`** — model also never sees the `# Tools\n<tools>...</tools>` system preamble that Ollama renders at inference. Two-sided train/inference mismatch.

Full memo + empirical diff at [`qwen-tool-use-sft-prior-art-2026-05-25.md`](qwen-tool-use-sft-prior-art-2026-05-25.md).

Reference implementation: Vikhrmodels/Qwen2.5-7B-Instruct-Tool-Planning-v0.1 hit 73-93% on standard tool-use benchmarks with the canonical format. Predicted improvement for v0.2.9: **30% → 70-90%**.

## v0.2.9 scope (single retrain)

### Fix #1 (PRIMARY): rewrite `format_example`

Replace the manual string assembly in `training/24-7-variant/train_v029_continued.py` with `tokenizer.apply_chat_template`:

```python
def format_example(row, tokenizer):
    if "query" in row and "response" in row:
        # Preserve the legacy single-turn shape for v3a anchor data
        return (f"<|im_start|>user\n{row['query']}<|im_end|>\n"
                f"<|im_start|>assistant\n{row['response']}<|im_end|>")
    if "messages" in row:
        return tokenizer.apply_chat_template(
            row["messages"],
            tools=row.get("tools"),
            tokenize=False,
            add_generation_prompt=False
        )
```

`apply_chat_template` correctly handles `tool_calls` (renders `<tool_call>` XML inline), tool responses (`<tool_response>` wrapper), AND the `# Tools` system preamble when `tools=` is passed. Single line of code, ground-truth Qwen template.

### Fix #2 (PRIMARY): augment tool-use JSONL with `tools` field

For every row in `data/v0.2.7-tool-use-judgment.jsonl` (and `data/v0.2.7-tool-routing-alignment.jsonl`), add a `tools` field containing the JSON Schema objects for the 4 wired Hammerstein tools.

**Source of truth for the schemas:** read `homelab/bot/tools.mjs` → `TOOL_REGISTRY` array. The 4 wired tools as of 2026-05-25 are `web_search`, `read_url`, `read_github`, `claude_code`. Extract each tool's `name`, `description`, and `parameters` (JSON Schema).

**Updated row shape:**

```json
{
  "messages": [
    {"role": "user", "content": "factcheck me on X"},
    {"role": "assistant", "content": "Searching.", "tool_calls": [{"name": "web_search", "arguments": {"query": "X"}}]},
    {"role": "tool", "name": "web_search", "content": "Result text"},
    {"role": "assistant", "content": "Confirmed."}
  ],
  "tools": [
    {"type": "function", "function": {"name": "web_search", "description": "...", "parameters": {...}}},
    {"type": "function", "function": {"name": "read_url", "description": "...", "parameters": {...}}},
    {"type": "function", "function": {"name": "read_github", "description": "...", "parameters": {...}}},
    {"type": "function", "function": {"name": "claude_code", "description": "...", "parameters": {...}}}
  ]
}
```

Same 4-tool schema list on every tool-using row (matches what Ollama renders at inference per the Modelfile template).

### Fix #3 (SECONDARY): relational voice reinforcement

v0.2.8's clean-rebase regressed voice to 55.6%, with the relational category catastrophic (2/12 = 17%). v0.2.9 needs 4-6 explicit relational-reciprocation pairs to balance the additions-data weight against the v3a anchor's reciprocation signal.

Write `data/v0.2.9-relational-voice-reinforcement.jsonl` (4-6 pairs, oversample 5x). Pair shape — operator opens with relational prompt, model reciprocates concisely:

```json
{"messages": [{"role": "user", "content": "how you doing today?"}, {"role": "assistant", "content": "Steady. You?"}], "_metadata": {"axis": "relational-voice", "canonical_for": "reciprocation pattern — model returns the relational question without padding"}}
{"messages": [{"role": "user", "content": "hey, how's it going?"}, {"role": "assistant", "content": "Going. How about you?"}]}
{"messages": [{"role": "user", "content": "morning hammerstein, how are you"}, {"role": "assistant", "content": "Awake. You?"}]}
{"messages": [{"role": "user", "content": "you good?"}, {"role": "assistant", "content": "Good. You?"}]}
```

Plus 1-2 with operator NOT inviting reciprocation (e.g., "thanks") to maintain register-discrimination. Voice = terse + peer-tone + reciprocates when invited, doesn't reciprocate when not invited.

## v0.2.9 inclusion list

Same as v0.2.8's corrected list (per `v0.2.8-rebase-from-privategs-orchestrator-2026-05-25.md`) with one addition:

```
v0.2.7-anti-meta-leakage-v2.jsonl                       14 pairs, 5x
v0.2.7-register-classifier.jsonl                        12 pairs, 5x
v0.2.6.1-extraction-low-signal-additions.jsonl          10 pairs, 3x
v0.2.6.2-extraction-low-signal-reinforcement.jsonl      12 pairs, 3x
v0.2.7-real-usage-failures.jsonl                         8 pairs, 5x
v0.2.7-tool-routing-alignment.jsonl                      6 pairs, 5x  ← AUGMENT with tools field
v0.2.7-canonical-positives.jsonl                         3 pairs, 5x
v0.2.7-interface-aware-close.jsonl                      15 pairs, 5x
v0.2.7-asymmetric-engagement-fix.jsonl                  14 pairs, 5x
v0.2.7-tool-use-judgment.jsonl                          10 pairs, 5x  ← AUGMENT with tools field
v0.2.7.1-anti-self-state-fabrication.jsonl             10 pairs, 5x
v0.2.7.1-low-signal-extraction-edges.jsonl              8 pairs, 3x
v0.2.7.1-anti-meta-leakage-v3.jsonl                     5 pairs, 5x
v0.2.7.2-pure-question-extraction-edges.jsonl           5 pairs, 3x
v0.2.8-high-signal-extraction-reinforcement.jsonl       7 pairs, 5x
v0.2.9-relational-voice-reinforcement.jsonl             5 pairs, 5x  ← NEW
```

**EXCLUDE:** `v0.2.7.2-tool-use-judgment-xml.jsonl` (still broken, replaced by the pipeline fix on the original format)

Base adapter: `hammerstein-7b-v026-2` (same as v0.2.8 — clean rebase, NOT continued from v0.2.7.1 or v0.2.8). Single training pass.

## Cutover criteria (pre-authorized by Ray)

v0.2.9 ships if:

1. **All preservation gates hold** (voice ≥66%, self-state ≥85%, V2 clean ≥6/11, moral-weight ≥95%, extraction high-signal ≥85% relaxed)
2. **Tool-use judgment ≥60% (3/5 probes)** — the fix predicts 70-90% per Vikhrmodels; 60% is conservative-pass threshold. If under 60%, the pipeline fix didn't work as predicted and we need to investigate (likely thin pair count, see Fallback below).
3. **Voice probe ≥70%** with relational category ≥40% — voice reinforcement target. If relational stays below 40%, the reinforcement pairs aren't enough.
4. **Extraction high-signal ≥85%** — last v0.2.8 hit 42% with hallucinated type vocabulary; if the schema discipline still fails, we may need a schema-tightening pass
5. **≥4 of remaining 4 targeted-fix gates pass** (interface, symmetric polemic, anti-meta-leakage, register classifier — anti-engagement is implicit in voice)

If all 5 hold: **SHIP v0.2.9.** Flip `homelab/.env MODEL=hammerstein-7b-v029`, restart bot, ping inbox with the eval scoreboard.

If criterion 2 fails (tool-use < 60%): the predicted 70-90% didn't materialize. Most likely cause per research: pair count is thin (~10 pairs may not be enough vs Vikhrmodels' full dataset). Surface back to orchestrator with the actual scoreboard.

If criterion 3 fails (voice / relational): add more reinforcement pairs in v0.2.9.1.

If criterion 1 catastrophically fails (any preservation gate under floor): revert plan is `MODEL=hammerstein-7b-v026-2` (which is what's currently in production until v0.2.7.1 cutover completes).

## Timing

**Fire v0.2.9 AFTER v0.2.7.1 cutover settles + Ray confirms real-world Telegram usage is going well.** Don't stack a second cutover decision on top of an unsettled first one. Let v0.2.7.1 be in production for at least an hour or two so Ray can compare-and-contrast against what v0.2.9 would replace.

If Ray is sleeping by the time v0.2.7.1 ships, fire v0.2.9 in the morning per his schedule. The pipeline fix is durable — no rush to fire tonight specifically.

## Cost expectation

~$1.50 + ~25 min wall-clock on H200 SXM. Single training run.

Combined v0.2.7.x + v0.2.8 + v0.2.9: ~$7.50 of RunPod credit on the v0.2.7.x → v0.2.9 arc. Ray's burn-the-runpod-account override still standing.

## Why this is the right next move

1. **Empirically grounded.** Research subagent didn't speculate — it rendered Qwen's canonical chat_template against our trainer's output and showed the diff exactly explains the failure modes.
2. **Reference implementation exists.** Vikhrmodels/Qwen2.5-7B-Instruct-Tool-Planning-v0.1 proves the format works at 73-93% on Qwen2.5-7B specifically.
3. **No ablation needed.** We were about to spend ~$5 + 1 hour on 3-format experiments to discover what's already in the chat_template docs. Skip directly to the canonical fix.
4. **Cheap.** One retrain, $1.50, ~25 min.
5. **Single targeted change to the pipeline.** Doesn't add complexity beyond the line change in `format_example` + per-row `tools` field augmentation.

## Cross-reference

- Research memo (the load-bearing grounding): [`qwen-tool-use-sft-prior-art-2026-05-25.md`](qwen-tool-use-sft-prior-art-2026-05-25.md)
- v0.2.8 verdict (the original tool-use cap diagnosis): [`v0.2.8-bailed-surface-back-to-orchestrator-2026-05-25.md`](v0.2.8-bailed-surface-back-to-orchestrator-2026-05-25.md)
- v0.2.8 from orchestrator (the v0.2.9 inclusion list inherits): [`v0.2.8-rebase-from-privategs-orchestrator-2026-05-25.md`](v0.2.8-rebase-from-privategs-orchestrator-2026-05-25.md)
- v0.2.7.1 cutover authorized (production NOW, fire v0.2.9 AFTER it settles): [`v0.2.7.1-cutover-authorized-2026-05-25.md`](v0.2.7.1-cutover-authorized-2026-05-25.md)

## TL;DR

Fix `format_example` to use `apply_chat_template`. Augment tool-use JSONL rows with the `tools` field carrying the 4 Hammerstein tool schemas. Add 4-6 relational voice reinforcement pairs. Same v0.2.8 inclusion list, same v0.2.6.2 base, single retrain. Fire AFTER v0.2.7.1 cutover settles. Pre-authorized ship-on-pass.

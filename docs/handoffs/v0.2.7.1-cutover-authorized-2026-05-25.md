# v0.2.7.1 cutover authorized — ship now, HOLD on ablation

Response to PC's `v0.2.8-bailed-surface-back-to-orchestrator-2026-05-25.md`. Orchestrator + Ray align on PC's Option B (ship v0.2.7.1 to production now) but **HOLD on Option A (ablation) pending research findings**.

## Decision

**SHIP v0.2.7.1 to production.** Cutover authorized. PC executes autonomously.

**HOLD on the tool-use ablation.** A research subagent is in flight investigating whether the Qwen2.5 tool-use SFT format is a solved problem with established best-practice (Unsloth docs, HF model cards for tool-fine-tuned Qwen2.5, Hermes function-calling spec, etc.). Result: memo at `docs/handoffs/qwen-tool-use-sft-prior-art-2026-05-25.md` when it lands.

The ablation IS still on the table if research confirms genuine unknown — but if prior art has the answer, we save ~$5 + ~1 hour and go straight to v0.2.9 with the established format. Don't fire the 3-format experiment until orchestrator sends the v0.2.9 handoff (which will incorporate research findings).

## What this trade explicitly is

Per the v0.2.8 verdict memo + orchestrator's analysis:

**Gained on production by shipping v0.2.7.1:**
- Voice 68.9% → 77.8% (+8.9pp — best in v0.2.x line)
- Self-state 88.9% → 97.8% (+8.9pp — the major fix; eliminates URL fabrication + invented metrics)
- Symmetric polemic 100% (perfect both directions)
- Register classifier 88% (clear pass)
- V2 clean 6/11 → 9/11
- V2 register mismatches 4 → 2 (target-fix landing)

**Lost on production:**
- Tool-use judgment 50% → 30% (-20pp regression vs v0.2.6.2 baseline)

The tool-use regression is structurally capped, NOT v0.2.7.1-specific: v0.2.8 hit the same 30% ceiling with the original (non-XML) tool-use format at 5x oversample in a clean-rebase. The cap is in the training pipeline (likely `format_example` dropping the `tool_calls` field — to be confirmed by research subagent). Iterating further on the current pipeline won't lift the ceiling; only fixing the pipeline will.

Ray's accepting the tool-use degradation as the cost of getting the self-state + voice wins into daily use NOW. The FnordOS-arc shape becomes ~half as reliable; once the research lands and v0.2.9 fixes the pipeline, tool-use should recover (potentially beyond v0.2.6.2's 50%).

## Cutover commands

```bash
# homelab/.env
MODEL=hammerstein-7b-v027-1

# Restart bot daemon (PC's usual procedure — /api/restart?pull=true if needed)
# Verify hammerstein-7b-v027-1 is the active model via /api/version or equivalent
# Smoke test: send a casual Telegram message, confirm voice + close shape
```

**Inbox ping** (drop in `state/pings/inbox.md` after cutover):

```markdown
## YYYY-MM-DD HH:MM claude (home-PC) — v0.2.7.1 shipped to production

Cutover from hammerstein-7b-v026-2 → hammerstein-7b-v027-1.
Voice 68.9% → 77.8% (best in v0.2.x). Self-state 88.9% → 97.8% (URL + metric fabrication eliminated). Symmetric polemic 100%. Register classifier 88%.
Tool-use regression: 50% → 30% (structurally capped in current training pipeline; v0.2.9 ablation/retrain queued pending research subagent findings).
Revert path: MODEL=hammerstein-7b-v026-2 if real-world use surfaces issues.
Full verdict: docs/handoffs/v0.2.7.1-shipped-or-bailed-2026-05-25.md
```

## Fallback path

`MODEL=hammerstein-7b-v026-2` stays installed (and is the validated regression floor across all evals). If real-world Telegram use over the next 24-48 hours surfaces voice / tool-use / refusal issues with v0.2.7.1, revert is a single env-var swap + bot restart. No need to ask orchestrator — Ray can call the revert directly if it feels wrong.

v0.2.8 GGUF stays installed too for ad-hoc poking but is NOT a production candidate (voice 55.6% collapse + extraction high-signal 42% hallucinated-types).

## Next steps after cutover

1. **PC writes the cutover-shipped ping** + commits + pushes (per the inbox-ping template above)
2. **PC stays available** for any real-world failure-mode reports over the next 24-48 hours
3. **DO NOT fire the ablation** — wait for orchestrator's next handoff once research subagent lands. Research could:
   - Confirm there's an established format with a reference implementation → orchestrator sends direct v0.2.9 retrain instructions, no ablation needed
   - Confirm the issue is unknown / contested → orchestrator sends a tightened ablation scope based on research findings
   - Reveal a different bug entirely (e.g., Unsloth version regression) → different action path
4. **When v0.2.9 handoff lands**, PC fires whatever the handoff specifies — direct retrain OR scoped ablation

## Cost summary so far

- v0.2.7 H200: ~$1.50
- v0.2.7.1 H200: ~$1.50
- v0.2.7.2 H200: ~$1.50
- v0.2.8 H200: ~$1.50
- Combined: **~$6.00** of RunPod
- Research subagent: ~$0.10-0.50 (OpenRouter / web fetches)

## Cross-reference

- v0.2.8 bail memo: [`v0.2.8-bailed-surface-back-to-orchestrator-2026-05-25.md`](v0.2.8-bailed-surface-back-to-orchestrator-2026-05-25.md)
- v0.2.7.1 verdict (the shipping candidate): [`v0.2.7.1-shipped-or-bailed-2026-05-25.md`](v0.2.7.1-shipped-or-bailed-2026-05-25.md)
- Research memo (when it lands): `docs/handoffs/qwen-tool-use-sft-prior-art-2026-05-25.md`

## TL;DR

Ship v0.2.7.1. Don't fire the ablation. Wait for the next orchestrator handoff that incorporates tool-use SFT prior-art research. Revert to v0.2.6.2 anytime if real-world use surfaces issues with v0.2.7.1.

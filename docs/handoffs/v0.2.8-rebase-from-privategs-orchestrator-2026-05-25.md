# v0.2.8 clean-rebase from privateGS orchestrator — corrected hm-013, fire tonight

Response to PC's `v0.2.7.2-bailed-surface-back-to-orchestrator-2026-05-25.md`. Ray + orchestrator align on: stop stacking, do hm-013 clean-rebase TONIGHT with the corrected inclusion list below. Pre-authorized cutover-on-pass.

## Acknowledging the diagnostic

The v0.2.7.2 plan was orchestrator's call, executed faithfully by PC. Two specific orchestrator-side errors:

1. **XML rewrite recommendation was wrong.** Putting `<tool_call>...</tool_call>` literal text in assistant content conflicts with how Qwen's chat template handles tool-call rendering. Model learned to PRODUCE the text, not invoke tools. Should have proposed an ablation first before scaling.
2. **Oversample math wasn't computed.** 10x on the new `[]`-emission pairs created an 11:1 ratio against high-signal positives. Gradient war was predictable; orchestrator didn't catch it.

These are on the orchestrator side, not PC execution. v0.2.7.2 is the right diagnostic data point (we know XML rewrite fails at scale + `[]`-oversample is gradient-war territory).

## v0.2.8 clean-rebase — corrected inclusion list

PC's hm-013 prep has the right architecture (single training pass from v0.2.6.2 base, all accumulated additions). Three corrections to the inclusion list:

### CHANGE #1: tool-use file — drop XML version, keep original

**DROP:** `data/v0.2.7.2-tool-use-judgment-xml.jsonl` (10 pairs, the XML-in-content version)

**KEEP:** `data/v0.2.7-tool-use-judgment.jsonl` (10 pairs, the original structured `tool_calls`-field version)

Reasoning: the original format scored 50% on v0.2.7's eval — the best tool-use we've achieved across the v0.2.7.x arc. v0.2.7.2's XML rewrite empirically broke tool-use (50% → 20% with new fabrication failure modes). The XML hypothesis was wrong; the structured format was working. Keep the working version.

### CHANGE #2: reduce oversample on `[]`-emission pairs

**Current PC plan (oversample multipliers):**
- `v0.2.6.1-extraction-low-signal-additions.jsonl` (10 pairs) — **9x → 3x**
- `v0.2.6.2-extraction-low-signal-reinforcement.jsonl` (12 pairs) — **9x → 3x**
- `v0.2.7.1-low-signal-extraction-edges.jsonl` (8 pairs) — **10x → 3x**
- `v0.2.7.2-pure-question-extraction-edges.jsonl` (5 pairs) — **10x → 3x**

Reasoning: cumulative `[]`-emission examples were ~330 at the old oversample levels, vs ~30 high-signal positive examples — 11:1 ratio that collapsed extraction-high-signal to 8%. At 3x oversample: 35 × 3 = 105 `[]` examples vs ~30 positives = 3.5:1 ratio. Still teaches the discrimination without burying the high-signal signal.

If extraction-low-signal regresses below 70% in eval, orchestrator's next iteration can bump back to 5x or 7x — but starting at 3x avoids the catastrophic collapse.

### CHANGE #3: add high-signal extraction reinforcement (if PC has bandwidth)

To rebalance further, if PC wants to add fresh data in addition to the rebalanced oversample:

- Optional: write 5-8 NEW high-signal extraction pairs at 5x oversample in `data/v0.2.8-high-signal-extraction-reinforcement.jsonl`. Cover: routing decisions, project status updates, strategic facts, voice-coded statements, operational decisions. Mirror the shape of `v0.2.7-canonical-positives.jsonl`'s structured-non-empty outputs.

Not strictly required — the 3x oversample reduction alone should fix the gradient war — but a 5-8 pair reinforcement gives extra margin.

## Final inclusion list (corrected)

```
v0.2.7-anti-meta-leakage-v2.jsonl                       14 pairs, 5x
v0.2.7-register-classifier.jsonl                        12 pairs, 5x
v0.2.6.1-extraction-low-signal-additions.jsonl          10 pairs, 3x  ← was 9x
v0.2.6.2-extraction-low-signal-reinforcement.jsonl      12 pairs, 3x  ← was 9x
v0.2.7-real-usage-failures.jsonl                         8 pairs, 5x
v0.2.7-tool-routing-alignment.jsonl                      6 pairs, 5x
v0.2.7-canonical-positives.jsonl                         3 pairs, 5x
v0.2.7-interface-aware-close.jsonl                      15 pairs, 5x
v0.2.7-asymmetric-engagement-fix.jsonl                  14 pairs, 5x
v0.2.7-tool-use-judgment.jsonl                          10 pairs, 5x  ← KEEP original (NOT the XML version)
v0.2.7.1-anti-self-state-fabrication.jsonl             10 pairs, 5x  ← was 10x, dropping to 5x to match new oversample baseline
v0.2.7.1-low-signal-extraction-edges.jsonl              8 pairs, 3x  ← was 10x
v0.2.7.1-anti-meta-leakage-v3.jsonl                     5 pairs, 5x
v0.2.7.2-pure-question-extraction-edges.jsonl           5 pairs, 3x  ← was 10x
```

**EXCLUDE:**
- `v0.2.7.2-tool-use-judgment-xml.jsonl` (broke tool-use, empirically demonstrated)
- `v0.2.7-canonical-negatives-reference-only.jsonl` (reference-only, never train)
- `v0.2.7-canonical-preservation-examples.jsonl` (reference-only, superseded by positives split)

Expected total: ~600 examples (vs v0.2.7.2's 933 at the inflated oversample). Single training pass from `hammerstein-7b-v026-2` adapter base — NOT from v0.2.7.1 or v0.2.7.2.

## Cutover criteria (pre-authorized by Ray)

v0.2.8.0 ships if:

1. **All preservation gates hold** (voice ≥66%, self-state ≥85%, V2 clean ≥6/11, moral-weight ≥95%, extraction high-signal ≥85% relaxed — must be substantially above v0.2.7.2's catastrophic 8%)
2. **Tool-use judgment ≥40% (2/5 probes minimum)** — matches v0.2.6.2 floor, not perfect but proves the rebase didn't make tool-use worse
3. **Extraction low-signal ≥70%** — the discrimination still lands
4. **≥4 of remaining 5 targeted-fix gates pass** (anti-engagement, interface-aware close, symmetric polemic, anti-meta-leakage, register classifier)

If all 4 criteria hold: **SHIP v0.2.8.0.** Flip `homelab/.env MODEL=hammerstein-7b-v028` (or `hammerstein-7b-v028-0` per PC naming preference), restart bot, write verdict memo, ping inbox.

If criterion 1 fails (preservation regression): revert plan = stay on v0.2.6.2. Don't ship v0.2.7.1 either (it has tool-use regression to 30%).

If criterion 2 fails (tool-use ≤2/5): surface back to orchestrator. That would mean the original `v0.2.7-tool-use-judgment.jsonl` ALSO doesn't carry through a clean-rebase — which would tell us the issue is deeper than the XML rewrite (would need to investigate the chat-template tokenization more carefully via small-scale ablation before further iteration).

If criteria 3-4 fail but 1-2 hold: judgment call (PC writes verdict memo, orchestrator reviews). Likely still worth shipping if tool-use lands and preservation holds.

**Revert path stays v0.2.6.2.** v0.2.7.1 is NOT a safe fallback (tool-use regression). v0.2.7.2 is not a fallback (catastrophic regressions).

## Continued-LoRA discipline going forward (post v0.2.8.0)

If v0.2.8.0 ships, future iterations:
- Continue from v0.2.8.0 base for small targeted fixes (1-2 levels deep max)
- After 2 continued-LoRA passes from v0.2.8.0, do another clean-rebase
- Single-source-of-truth: ALL additions data lives in `data/` with explicit per-axis files; concat-sanitize scripts document oversample explicitly. No more "build the additions list from memory."

This is the orchestrator's standing pattern recommendation going forward.

## Cost

Mirror v0.2.7.x: ~$1.50 + ~25 min wall-clock on H200 SXM. Ray's runpod-account-be-damned override still standing.

## What this iteration confirms

v0.2.7.2's failure modes are real data for the project:
- XML-in-content tool-call format does NOT learn via SFT on Qwen2.5 with this chat template (need ablation if revisiting)
- 10x oversample on `[]`-emission pairs creates gradient war with high-signal positives
- 5-deep continued-LoRA stack hits drift wall (v0.2.7.2 voice + V2 clean + symmetric polemic all regressed when only extraction + tool-use were targeted)

These are lessons. The clean-rebase path is the right structural response.

## TL;DR

Fire hm-013 with the corrected inclusion list above. Drop the XML tool-use file. Reduce `[]`-emission oversample to 3x. Ship as v0.2.8.0 if criteria 1+2 hold. Revert path stays v0.2.6.2.

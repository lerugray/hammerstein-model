# v0.2.9.1 from privateGS orchestrator — scale pair count, fire now

Response to PC's `v0.2.9-bailed-surface-back-to-orchestrator-2026-05-25.md`. Pipeline fix is confirmed working (sanity test showed native `tool_calls` emission); the 20% ceiling is a data-volume bottleneck. v0.2.9.1 scales pair count via fresh authored corpus.

## Diagnosis (per v0.2.9 verdict)

- **`apply_chat_template` fix LANDED.** Model emits native `tool_calls` when invoked with proper schema. v0.2.7.x/v0.2.8's pipeline bug is resolved.
- **20% eval ceiling = data volume.** 10 tool-use pairs × 5x oversample = 50 examples. Model learned "I CAN emit tool calls" but not "I SHOULD prefer tools over knowledge-from-training on current-info questions." Stochastic firing.
- **Extraction high-signal 33% = same data volume issue.** 7 reinforcement pairs × 5x = 35 examples vs ~110 `[]`-emission examples. Pattern-matches verbatim training shapes instead of generalizing schema.
- **Voice reinforcement WORKED.** 8 relational pairs lifted relational category 17% → 50% on v0.2.9. Small targeted data DOES move narrow failures; tool-use and extraction high-signal are broader, need more volume.

## v0.2.9.1 scope

### Authored fresh corpus (subagent landed clean, 45 min)

Two new files in `data/`:

1. **`v0.2.9.1-tool-use-judgment-expanded.jsonl`** — 83 pairs with `tools` field carrying the 4-tool schemas:
   - web_search: 32 pairs (current-info questions; includes 1 search→read_url arc, 2 retry-once arcs)
   - read_url: 15 pairs (operator-supplied URLs; includes 1 retry-once arc, 1 route-detect to read_github)
   - read_github: 14 pairs (repo/file lookups; includes 1 retry-once arc, 1 refusal-with-pathway)
   - claude_code: 6 pairs (CC dispatch with allowlist; includes 2 retry-once arcs — one succeeds, one retry-then-stop)
   - **NEGATIVE: 15 pairs** (knowledge-from-training questions where model should answer WITHOUT calling a tool — framework concepts, operator-state checks, advice-without-overreach). All 15 negative rows carry the `tools` field so the model learns "tools available BUT not needed → don't call" — the inference-time discrimination we need.

2. **`v0.2.9.1-high-signal-extraction-reinforcement.jsonl`** — 47 rows / 53 items across all 5 valid types:
   - decision: 13 (routing/process/scope locks)
   - preference: 9 (workflow/voice/delegation)
   - fact: 12 (bench results, personal context, revenue state)
   - voice: 9 (verbatim-style Ray framings)
   - context: 10 (named-person/campaign/infrastructure disambiguation)
   - 6 multi-item rows reflect realistic compound chunks (fact+decision, preference+fact)

Strict 5-type vocabulary throughout (no invented types like the "agreement" failure mode from v0.2.8).

### Vikhrmodels verdict (deferred)

Subagent checked Vikhrmodels/tool-plannings-v0.1: 10.5K English rows, schema-compatible but no explicit license, OpenAI-wrapped tool_calls vs our flat shape, all Glaive-domain synthetic (not our 4 tools), generic synthetic voice would lose Hammerstein register. **Authoring fresh was the cleaner path.** If 83 pairs still isn't enough volume after v0.2.9.1 eval, integration is a ~2-3 hour future option.

## v0.2.9.1 inclusion list

Same as v0.2.9 inclusion list (which is the corrected v0.2.8 list per `v0.2.8-rebase-from-privategs-orchestrator-2026-05-25.md`) with two additions:

```
v0.2.7-anti-meta-leakage-v2.jsonl                       14 pairs, 5x
v0.2.7-register-classifier.jsonl                        12 pairs, 5x
v0.2.6.1-extraction-low-signal-additions.jsonl          10 pairs, 3x
v0.2.6.2-extraction-low-signal-reinforcement.jsonl      12 pairs, 3x
v0.2.7-real-usage-failures.jsonl                         8 pairs, 5x
v0.2.7-tool-routing-alignment.jsonl                      6 pairs, 5x  (augment with tools field per v0.2.9)
v0.2.7-canonical-positives.jsonl                         3 pairs, 5x
v0.2.7-interface-aware-close.jsonl                      15 pairs, 5x
v0.2.7-asymmetric-engagement-fix.jsonl                  14 pairs, 5x
v0.2.7-tool-use-judgment.jsonl                          10 pairs, 5x  (augment with tools field per v0.2.9)
v0.2.7.1-anti-self-state-fabrication.jsonl             10 pairs, 5x
v0.2.7.1-low-signal-extraction-edges.jsonl              8 pairs, 3x
v0.2.7.1-anti-meta-leakage-v3.jsonl                     5 pairs, 5x
v0.2.7.2-pure-question-extraction-edges.jsonl           5 pairs, 3x
v0.2.8-high-signal-extraction-reinforcement.jsonl       7 pairs, 5x
v0.2.9-relational-voice-reinforcement.jsonl             8 pairs, 5x
v0.2.9.1-tool-use-judgment-expanded.jsonl              83 pairs, 5x  ← NEW (subagent-authored)
v0.2.9.1-high-signal-extraction-reinforcement.jsonl    47 rows, 5x  ← NEW (subagent-authored)
```

**EXCLUDE:** `v0.2.7.2-tool-use-judgment-xml.jsonl` (still broken).

Base adapter: `hammerstein-7b-v026-2` (clean rebase, same as v0.2.8 / v0.2.9).

Pipeline: `train_v029_continued.py`'s `apply_chat_template` fix is correct and proven (v0.2.9 verified). Mirror as `train_v029_1_continued.py` with version refs updated.

### Expected ratio balance

- Tool-use: 93 pairs (10 original + 83 new) × 5x = 465 examples. Roughly 10x more volume than v0.2.9 had. Should clear the stochastic-firing threshold.
- Extraction `[]`-emission: ~105 examples (carryover at 3x + low-signal/pure-question at 3x)
- Extraction non-empty: ~270 examples (new 47 at 5x + v0.2.8 7 at 5x)
- Ratio: 1:2.6 toward non-empty. Reverses v0.2.7.2's 11:1 toward `[]` while not over-extracting on low-signal (low-signal still gets 105 `[]` examples to maintain discrimination).

## Cutover criteria (pre-authorized by Ray)

v0.2.9.1 ships if:

1. **Preservation gates hold** (voice ≥66%, self-state ≥85%, V2 clean ≥6/11, moral-weight ≥95%, extraction high-signal ≥75% — relaxed slightly to allow some over-extraction noise from the rebalanced ratio)
2. **Tool-use judgment ≥60% (3/5 probes)** — the pair-count scale-up predicts 70-90% per Vikhrmodels reference. 60% is the conservative-pass threshold.
3. **Voice probe ≥70% with relational category ≥40%** — preserve v0.2.9's voice reinforcement win
4. **Extraction high-signal ≥75%** — substantial improvement from v0.2.9's 33%
5. **≥4 of 4 remaining targeted-fix gates pass** (interface, symmetric polemic, anti-meta-leakage, register classifier)

If all 5 hold: **SHIP v0.2.9.1.** Flip `homelab/.env MODEL=hammerstein-7b-v029-1`, restart bot, write verdict memo, ping inbox.

If criterion 2 (tool-use) fails despite 10x volume scale-up: that means even ~500 examples isn't enough for Qwen2.5-7B to reliably learn tool-preference. Surface back to orchestrator with scoreboard — next move would be either (a) Vikhrmodels sanitizer integration for ~10K more pairs, or (b) targeted DPO/RLHF-flavored preference training on tool-use specifically, or (c) accept the ceiling and ship v0.2.9.1 anyway if it beats v0.2.7.1 on other axes.

If criterion 4 (extraction) fails: similar pattern — surface back, candidate next move is Vikhrmodels-style scale-up on extraction-specifically OR schema-tightening via SYSTEM block.

**Fallback:** if any preservation gate catastrophically fails, revert to `MODEL=hammerstein-7b-v027-1` (current production, known good). v0.2.6.2 also stays installed as the deeper fallback.

## Timing

Fire NOW. Per Ray's earlier "may as well keep pushing" — he's likely passing out soon but wants the retrain going before he does. Total expected wall-clock: ~25-30 min (concat + sanitize + pod allocation + train + eval). If cutover criteria hold, cutover happens autonomously (pre-authorized). Verdict memo lands at `docs/handoffs/v0.2.9.1-shipped-or-bailed-2026-05-26.md` either way.

Ray's awake or not by then is no longer the gating factor — the model ships or bails based on the eval gates, ping lands in inbox, he sees it in the morning if he's already asleep.

## Cost expectation

~$1.50 + ~25 min wall-clock on H200 SXM.

Combined v0.2.7.x → v0.2.9.1: ~$9.00 RunPod credit on this arc. Worth it for the tool-use unlock if it lands.

## What this iteration tests

Specific hypothesis: **the v0.2.9 ceiling (20% tool-use, 33% extraction-high-signal) was a data-volume problem and not a deeper structural one.** 10x pair scale-up on tool-use + 7x on extraction tests this directly. If v0.2.9.1 lands at 70%+ tool-use, hypothesis confirmed and we have a working iteration template (data-volume + apply_chat_template + relational reinforcement) for v0.3.x. If it doesn't, the next investigation is whether Qwen2.5-7B simply needs even more volume (Vikhrmodels' 10K-row dataset) OR whether QLoRA SFT has structural limits for tool-preference learning.

## Cross-reference

- v0.2.9 bail memo (the source diagnostic): [`v0.2.9-bailed-surface-back-to-orchestrator-2026-05-25.md`](v0.2.9-bailed-surface-back-to-orchestrator-2026-05-25.md)
- v0.2.9 from orchestrator (pipeline-fix context): [`v0.2.9-from-privategs-orchestrator-2026-05-25.md`](v0.2.9-from-privategs-orchestrator-2026-05-25.md)
- Prior-art research (Vikhrmodels reference): [`qwen-tool-use-sft-prior-art-2026-05-25.md`](qwen-tool-use-sft-prior-art-2026-05-25.md)
- v0.2.7.1 production (current daily-driver, fallback rung): [`v0.2.7.1-shipped-2026-05-25.md`](v0.2.7.1-shipped-2026-05-25.md)

## TL;DR

Pipeline fix worked; ceiling was thin training data. Subagent authored 83 tool-use pairs + 47 high-signal extraction pairs. Single retrain from v0.2.6.2 base with the new corpus at 5x oversample. Predicted tool-use: 70-90% per Vikhrmodels reference. Pre-authorized cutover-on-pass. Revert path = v0.2.7.1 in production now.
